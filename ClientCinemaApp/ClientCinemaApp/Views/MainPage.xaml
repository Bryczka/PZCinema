<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ClientCinemaApp"
             x:Class="ClientCinemaApp.MainPage" Title="List of films">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Tickets" Clicked="ToolbarItem_Clicked_1"/>
        <ToolbarItem Text="About" Clicked="ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="filmsListView" BackgroundColor="DimGray">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell Tapped="FilmItemText_Tapped" Height="50">
                            <Grid>
                                <Label x:Name="FilmItemText" TextColor="#FFFF02" Text="{Binding Title}"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
